{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongodb": {
    "host": "${MONGODB_HOST}",
    "port": "${MONGODB_PORT}",
    "url": "",
    "database": "${MONGODB_NAME}",
    "password": "${MONGODB_PASSWORD}",
    "name": "mongodb",
    "user": "${MONGODB_USER}",
    "connector": "mongodb"
  }
}
